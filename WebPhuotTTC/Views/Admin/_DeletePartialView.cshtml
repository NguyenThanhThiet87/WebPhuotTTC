@model WebPhuotTTC.Models.LOAISANPHAM
<link href="../Content/AdminPage/PartialView/Category/DeletePartialView.css" rel="stylesheet" />
<div class="admin-category-dialog_delete">
    <div>
        <i class="fa-solid fa-rectangle-xmark" style="color: black;" onclick="CloseDialog(event)"></i>
        <h2>Delete</h2>
        <hr/>
        <p class="title">Bạn có chắc chắn muốn xóa</p>
        <p><span>Mã Loại:</span> @Model.MaLoai</p>
        <p><span>Tên Loại:</span> @Model.TenLoai</p>
        @using (Html.BeginForm("Delete", "Admin", "POST"))
        {
            <input id="submit" type="submit" value="Delete" />
        }
    </div>
</div>